declare_args() {
  # C language  standard version.
  std_c = "c11"

  # extra C compile flags.
  extra_cflags_c = []

  # extra C defines.
  extra_defines_c = []

  # static linking to libc.
  use_static_libc = false
}
